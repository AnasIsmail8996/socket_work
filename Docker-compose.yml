version: '3.9'

services:
  backend:
    build: ./server           
    container_name: backend
    restart: always
    env_file:
      - ./server/.env       
    ports:
      - "5000:5000"         

  frontend:
    build:
      context: ./client
      args:
        VITE_API_URL: "https://13.51.48.205:5000/api"   
    container_name: frontend
    restart: always
    ports:
      - "3000:5173"         
